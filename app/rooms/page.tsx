import { ArrowRight, Check, Phone } from 'lucide-react'
import { hotel, images, phoneHref, rooms } from '@/lib/hotel-data'
import { Header, Footer, MobileActions, Hero, ReservationForm, SectionHeading } from '@/components/hotel-site'
export const metadata = { title: 'Rooms & Accommodation | Cloud Four Hotel Limited', description: 'Explore the available accommodation options at Cloud Four Hotel Limited in Oshodi, Lagos.' }
export default function Rooms() { return <><Header/><main><Hero eyebrow="Accommodations" title={<>Rooms made<br/><em className="text-gold">for rest & comfort.</em></>} copy="Discover our available accommodation options and choose the space that best suits your stay." image={images.bedroom} cta="Send an Enquiry" secondary="View Gallery"/ ><section className="bg-ivory px-5 py-20 lg:px-8 lg:py-28"><div className="mx-auto max-w-7xl"><SectionHeading light eyebrow="The rooms" title="A comfortable place to land"/><div className="mt-16 flex flex-col gap-20">{rooms.map((room, i) => <article key={i} className="grid items-center gap-10 lg:grid-cols-2 lg:gap-20"><div className={`${i % 2 ? 'lg:order-2' : ''} relative aspect-[5/4] overflow-hidden`}><div className="flex h-full snap-x snap-mandatory overflow-x-auto overscroll-x-contain scroll-smooth [scrollbar-width:none] [&::-webkit-scrollbar]:hidden" aria-label={`${room.title} image gallery`}>{('gallery' in room && room.gallery ? room.gallery : [room.image]).map((src, imageIndex) => <div key={src} className="h-full min-w-full snap-center"><img src={src} alt={`${room.title} room interior view ${imageIndex + 1}`} className="h-full w-full object-cover transition duration-700 hover:scale-105"/></div>)}</div>{'gallery' in room && room.gallery && <div className="pointer-events-none absolute bottom-4 right-4 flex gap-2" aria-label="Swipe for more room images">{room.gallery.map((src, imageIndex) => <span key={src} className="h-1.5 w-8 rounded-full bg-ivory/80" aria-label={`Image ${imageIndex + 1}`}/>)}</div>}</div><div className={i % 2 ? 'lg:order-1' : ''}><p className="eyebrow text-charcoal/60">Accommodation 0{i + 1}</p><h2 className="mt-4 font-serif text-4xl text-charcoal">{room.title}</h2><p className="mt-3 font-mono text-sm text-gold">₦{room.price} / night</p><p className="mt-6 max-w-md leading-7 text-charcoal/65">{room.description}</p><ul className="mt-7 grid gap-3 text-sm text-charcoal/70 sm:grid-cols-2">{room.amenities.map((a, j) => <li key={j} className="flex items-center gap-2"><Check size={15} className="text-gold"/>{a}</li>)}</ul><a href="#reservation" className="dark-button mt-8">Reserve this room <ArrowRight size={16}/></a></div></article>)}</div></div></section><section className="bg-charcoal px-5 py-20 lg:px-8 lg:py-28"><div className="mx-auto grid max-w-7xl gap-12 lg:grid-cols-[0.8fr_1.2fr]"><div><SectionHeading eyebrow="Reservation enquiry" title="Ready to stay with us?" copy="Share your preferred dates and we will be in touch. This enquiry form is ready to connect to your preferred booking or email workflow."/><div className="mt-8 flex items-center gap-3 text-sm text-muted-ivory"><Phone size={16} className="text-gold"/> Prefer to speak directly? <a className="text-gold" href={phoneHref(hotel.phones[0])}>{hotel.phones[0]}</a></div></div><ReservationForm/></div></section></main><Footer/><MobileActions/></> }
